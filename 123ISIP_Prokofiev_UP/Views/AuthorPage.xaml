<Page x:Class="_123ISIP_Prokofiev_UP.Views.AuthorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      FontFamily="Segoe UI" Background="{StaticResource PageBackground}"
      Title="Страница автора">
    <Grid Margin="32,28,32,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="Мои книги" Style="{StaticResource H1}" VerticalAlignment="Center"/>
            <Button Content="＋ Добавить книгу" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right"
                    VerticalAlignment="Center" Click="Add_Click"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,18,0,0" Padding="0,0,8,0">
            <StackPanel>
                <TextBlock x:Name="EmptyText" Text="Вы ещё не опубликовали ни одной книги." Style="{StaticResource Muted}" Visibility="Collapsed"/>
                <ItemsControl x:Name="BooksItems">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="60" Height="84" CornerRadius="8"
                                            Background="{Binding Converter={StaticResource CoverConv}, ConverterParameter=brush}">
                                        <Path Data="{StaticResource Icon.Book}" Fill="#55E8E4DD" Width="28" Height="28" Stretch="Uniform"
                                              Visibility="{Binding Converter={StaticResource CoverConv}, ConverterParameter=icon}"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                                            <Border Background="#FBE3DC" CornerRadius="6" Padding="7,2" Margin="10,0,0,0"
                                                    Visibility="{Binding IsFrozen, Converter={StaticResource BoolToVis}}">
                                                <TextBlock Text="❄ заморожена" Foreground="{StaticResource AccentDarkBrush}" FontSize="11" FontWeight="SemiBold"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding GenresText}" Style="{StaticResource Muted}" Margin="0,4,0,0"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <Path Data="{StaticResource Icon.Star}" Fill="{StaticResource StarBrush}" Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding AvgRatingText}" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" Margin="4,0,2,0"/>
                                            <TextBlock Text="{Binding ReviewsCount, StringFormat=({0} отз.)}" Style="{StaticResource Muted}" FontSize="12" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <Button Content="Редактировать" Style="{StaticResource SecondaryButton}" Click="Edit_Click" Margin="0,0,0,6"/>
                                        <Button Content="Оспорить заморозку" Style="{StaticResource SecondaryButton}" Click="Contest_Click"
                                                Visibility="{Binding IsFrozen, Converter={StaticResource BoolToVis}}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
